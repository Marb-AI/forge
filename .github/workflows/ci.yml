name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      # The desktop shell is cgo over GTK and WebKit, so ./... does not compile
      # without them — and the point of installing them here rather than excluding
      # the package is that `go vet ./...` keeps meaning all of it.
      - name: desktop shell build deps
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-4-dev libwebkitgtk-6.0-dev libx11-dev

      - name: gofmt
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "These files need gofmt:"; echo "$unformatted"; exit 1
          fi

      - name: vet
        run: go vet ./...

      - name: test
        run: go test -race -coverprofile=coverage.out ./...

      - name: coverage total
        run: go tool cover -func=coverage.out | tail -1

      - name: cross-compile all platforms
        run: |
          set -e
          for t in darwin/amd64 darwin/arm64 linux/amd64 linux/arm64 windows/amd64 windows/arm64; do
            echo "client $t"
            GOOS=${t%/*} GOARCH=${t#*/} go build -o /dev/null ./cmd/forge
          done
          for a in amd64 arm64; do
            echo "agent linux/$a"
            GOOS=linux GOARCH=$a go build -o /dev/null ./cmd/forge-agent
          done
